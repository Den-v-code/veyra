# VAM compiler

The first Core Language lowering is implemented in `../src/compiler.py`.

Docs: `../docs/006_core_lowering.md`.

Current supported subset: finite `echo/mode/breath/tact/nod/observer` expressions with optional certificate emission.
